services:
  # Nginx Reverse Proxy with automatic SSL
  nginx-proxy:
    image: nginxproxy/nginx-proxy:latest
    container_name: nginx-proxy
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
      - certs:/etc/nginx/certs:ro
      - vhost:/etc/nginx/vhost.d
      - html:/usr/share/nginx/html
      - conf:/etc/nginx/conf.d
    networks:
      - pronova-network

  # Let's Encrypt SSL Certificate Automation
  acme-companion:
    image: nginxproxy/acme-companion:latest
    container_name: acme-companion
    restart: always
    depends_on:
      - nginx-proxy
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - certs:/etc/nginx/certs:rw
      - vhost:/etc/nginx/vhost.d
      - html:/usr/share/nginx/html
      - acme:/etc/acme.sh
    environment:
      - DEFAULT_EMAIL=no-reply@pronovacrypto.tech
      - NGINX_PROXY_CONTAINER=nginx-proxy
    networks:
      - pronova-network

  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: pronova-db
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER:-pronova}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-PronovaDB2026Secure}
      POSTGRES_DB: ${DB_NAME:-pronova}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - pronova-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pronova"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: pronova-redis
    restart: always
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - pronova-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend API Service
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: pronova-backend
    restart: always
    environment:
      - NODE_ENV=production
      - PORT=5000
      - DATABASE_URL=postgresql://${DB_USER:-pronova}:${DB_PASSWORD:-PronovaDB2026Secure}@postgres:5432/${DB_NAME:-pronova}
      - REDIS_URL=redis://redis:6379
      - JWT_SECRET=${JWT_SECRET:-pronova-jwt-secret-2026-secure-key}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET:-pronova-refresh-secret-2026-secure-key}
      - FRONTEND_URL=https://pronovacrypto.com
      - SMTP_HOST=smtp.hostinger.com
      - SMTP_PORT=465
      - SMTP_USER=${SMTP_USER:-no-reply@pronovacrypto.tech}
      - SMTP_PASS=${SMTP_PASS}
      - VIRTUAL_HOST=api.pronovacrypto.com,api.pronovacrypto.tech
      - VIRTUAL_PORT=5000
      - LETSENCRYPT_HOST=api.pronovacrypto.com,api.pronovacrypto.tech
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - pronova-network

  # Frontend React Application
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
      args:
        - REACT_APP_API_URL=https://api.pronovacrypto.com/api
        - REACT_APP_WS_URL=https://api.pronovacrypto.com
        - REACT_APP_CHAIN_ID=97
        - REACT_APP_NETWORK_NAME=BSC Testnet
        - REACT_APP_TOKEN_CONTRACT_ADDRESS=0x646937084d5f5Aba37f5e1014E9E7bbE90032dE1
        - REACT_APP_PRESALE_CONTRACT_ADDRESS=0x1AE86fB64059723a9e8c3C975D3095Aa5dca05Ba
        - REACT_APP_VESTING_CONTRACT_ADDRESS=0x779D973900B2294CA8fe063da39239Cd8bA13a2E
        - REACT_APP_NAME=Pronova
        - REACT_APP_VERSION=1.0.0
    container_name: pronova-frontend
    restart: always
    environment:
      - VIRTUAL_HOST=pronovacrypto.com,www.pronovacrypto.com,pronovacrypto.tech,www.pronovacrypto.tech
      - VIRTUAL_PORT=80
      - LETSENCRYPT_HOST=pronovacrypto.com,www.pronovacrypto.com,pronovacrypto.tech,www.pronovacrypto.tech
    depends_on:
      - backend
    networks:
      - pronova-network

networks:
  pronova-network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  certs:
  vhost:
  html:
  conf:
  acme:
